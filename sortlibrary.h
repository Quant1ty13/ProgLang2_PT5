#ifndef SORTLIBRARY_H // we will define sortlibrary.h to create our own functions and library
#define SORTLIBRARY_H

void Ascending(int array[], int n); // array, number of items inside the array

void Descending(int array[], int n);

#endif